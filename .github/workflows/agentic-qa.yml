name: AgenticQA Pipeline
on:
  workflow_dispatch:
    inputs:
      pipeline_type:
        description: 'Pipeline type'
        required: false
        default: 'full'
jobs:
  agentic-qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run AgenticQA
        run: echo "AgenticQA pipeline running on ${{ github.ref_name }}"